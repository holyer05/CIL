#!/usr/bin/env bash
set -u

branch=$(git branch --show-current)
if [[ "$branch" != "main" ]]; then
  exit 0
fi

if ! git push origin "$branch"; then
  echo "Warning: commit created locally, but automatic GitHub push failed." >&2
fi
