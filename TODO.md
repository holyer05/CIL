# TODO.md

> 当前可执行任务列表。任务必须包含验收条件。最后更新：2026-06-25。

## P0：立即执行

- [ ] `ENV-001` 固化运行环境
  - 输出 Python、PyTorch、torchvision、CUDA、cuDNN 和 GPU 信息。
  - 生成最小依赖说明或环境文件。
  - 验收：新 shell 中可导入核心依赖并运行 `PyCIL/main.py` 的参数解析。

- [ ] `DATA-001` 审计可用数据集
  - 检查 `/root/autodl-pub` 中 CIFAR-100、ImageNet-100 等实际目录结构。
  - 只建立路径配置或软链接，不复制公共数据到 Git 工作区。
  - 验收：数据加载器可读取样本并报告类别数、训练集和测试集规模。

- [ ] `BASE-001` 选定首个最小基线协议
  - 优先选择低成本数据集和 PyCIL 已有配置。
  - 明确任务数、initial increment、increment、seed、骨干和评价指标。
  - 验收：协议写入配置，并在 `EXPERIMENT_LOG.md` 建立 planned 条目。

- [ ] `BASE-002` 完成 smoke test
  - 先运行最小 epoch/最少任务验证端到端流程。
  - 验收：训练、评估、日志和结果保存均成功；失败则记录完整错误。

## P1：基线与研究诊断

- [ ] `BASE-003` 复现至少一个简单 EFCIL baseline。
- [ ] `BASE-004` 复现至少一个强 EFCIL baseline，并核对公开结果协议。
- [ ] `DIAG-001` 为 `IDEA-001` 设计低成本漂移子空间诊断实验。
- [ ] `LIT-001` 对 IDEA-001 至 IDEA-003 做最近工作和重合风险审计。
- [ ] `METRIC-001` 统一记录平均准确率、最终准确率、遗忘和每任务曲线。

## P2：方法开发准入

仅当某个想法通过 `IDEA_POOL.md` 的准入标准后执行：

- [ ] 定义独立配置开关，默认关闭。
- [ ] 写出 baseline 等价性检查。
- [ ] 完成单元或最小数值测试。
- [ ] 先做单数据集、单 seed 的机制验证。
- [ ] 机制成立后再扩展多 seed、多数据集和完整消融。

## 维护规则

- 完成任务时勾选，并在相应实验或提交记录中给出证据。
- 新任务使用稳定 ID，不复用已完成或已取消 ID。
- 被取消的任务保留并标注原因，不直接删除。
- 每次工作结束前重新排序 P0/P1/P2，确保下一步明确。
